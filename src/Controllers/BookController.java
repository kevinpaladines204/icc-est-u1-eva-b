package Controllers;

public class BookController {
    public void sortByName(Book[] libros) {
        for(i=0; i<Book.length;i++){
            Book[i]=[i+1];
            [i+1]=aux;
            aux=j;
        }
        while(i=0; i>=Book.length;i++){
            Book[i]=[j+1];
            [j+1]=aux;
            aux=i;
        }
        Book[j+1];
    }
    
    public Book searchByName(Book[] libros, String name) {
        for(Book.getName()>Books.length){
            if(Book){
                Book.getName()= Book[j]
            }

        }
        while{

        }

        return null;
    }
    
}